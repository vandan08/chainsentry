/**
 * Read-model REST API for the dashboard UI: org overview, findings backlog,
 * trends. Deliberately query-only — writes go through their owning modules. Phase 4.
 */
package io.chainsentry.dashboard;
